name: "BESS Manager (fork)"
description: "Battery Energy Storage System optimization and management"
version: "9.8.1-dev.8"
slug: "bess_manager"
image: "ghcr.io/pra/bess-manager-{arch}"
init: false
arch:
  - aarch64
startup: application
boot: auto
ports:
  8080/tcp: 8080
ingress: true
ingress_port: 8080
panel_icon: mdi:battery-charging
panel_title: BESS Manager
panel_admin: true
hassio_api: true
homeassistant_api: true
auth_api: true
options:
  influxdb:
    url: "http://homeassistant.local:8086/api/v2/query"
    bucket: "home_assistant/autogen"
    username: "your_db_username_here"
    password: "your_db_password_here"
schema:
  influxdb:
    url: str
    bucket: str
    username: str
    password: str
